# you must fill it. Don't commit the file
database:
  default:
    login: your_bd_login
    password: your_bd_password

admin:
  default:
    login: your_admin_login
    password: your_admin_password

token:
  default:
    secret: your_secret
    lifetime: your_lifetime